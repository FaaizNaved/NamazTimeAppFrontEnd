/** How many days of local adhan alarms to schedule on each full refresh. */
export const SCHEDULE_AHEAD_DAYS = 30;

/** Minimum days kept up to date by background refresh (daily task). */
export const BACKGROUND_REFRESH_MIN_DAYS = 7;
